# Optional: enables AI review/refinement of the locally generated multi-agent care plan.
# Without this key, the app still generates a dynamic local care plan from assessment answers.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
